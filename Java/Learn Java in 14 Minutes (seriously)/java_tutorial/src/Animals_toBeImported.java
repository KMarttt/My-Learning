public class Animals_toBeImported {
    public static String iAmDog() {
        // there is no (String s) because this code will not
        // use string?
        return "I am a dog";

        // each object has its own methods (oop)
        // each class represent an object which has its own method
    }
}
